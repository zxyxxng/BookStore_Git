spring:
#  datasource:
#    url: jdbc:h2:mem:test # 1. 테스트 할때 db띄울 필요 없이 jvm내에 memory에서 db를 돌릴 수 있는 h2의 url
#    username: sa
#    password:
#      driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop  # 애플리케이션 실행시점에 기존 엔티티 다 지우고 다시 테이블 자동 생성
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

# 2. 하지만 스프링 부트에서는 별도의 설정이 없으면 기본적으로 메모리 모드에서 돌려버림... (jdbc:h2:mem:98ede657-17cd-4f0c-a2ca-fcdc18ad5837)
logging:
  level:
    org.hibernate.SQL: debug  # 운영에서 sql은 logger를 통해서 찍어야함
    org.hibernate.orm.jdbc.bind: trace  # 쿼리 파라미터 남겨주는 로그(spring boot 3.x 이상, hibernate6 이상)